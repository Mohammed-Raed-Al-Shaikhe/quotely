@extends('layout',['title'=>'Workspace settings · Quotely']) @section('content')<div style="max-width:750px;margin:auto"><div class="top"><div><h1>Workspace settings</h1><p>Brand and financial defaults used on new documents.</p></div><a class="btn secondary" href="{{ route('billing') }}">Billing</a></div><form class="card" method="post" action="{{ route('settings.update') }}">@csrf @method('put')<label>Business name</label><input name="name" value="{{ old('name',$company->name) }}"><label>Business email</label><input type="email" name="email" value="{{ old('email',$company->email) }}"><label>Address</label><textarea name="address">{{ old('address',$company->address) }}</textarea><div class="grid"><div><label>Currency</label><input name="currency" maxlength="3" value="{{ old('currency',$company->currency) }}"></div><div><label>Tax label</label><input name="tax_name" value="{{ old('tax_name',$company->tax_name) }}"></div><div><label>Tax rate %</label><input type="number" min="0" max="100" name="tax_rate" value="{{ old('tax_rate',$company->tax_rate) }}"></div></div>@if($errors->any())<div class="error">{{ $errors->first() }}</div>@endif<button class="btn" style="margin-top:20px">Save settings</button></form></div>@endsection
