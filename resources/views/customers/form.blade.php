@extends('layout',['title'=>($customer->exists?'Edit':'New').' customer · Quotely']) @section('content')<div style="max-width:700px;margin:auto"><div class="top"><h1>{{ $customer->exists?'Edit':'New' }} customer</h1><a class="btn secondary" href="{{ route('customers.index') }}">Back</a></div><form class="card" method="post" action="{{ $customer->exists?route('customers.update',$customer):route('customers.store') }}">@csrf @if($customer->exists)@method('put')@endif<label>Contact name</label><input name="name" value="{{ old('name',$customer->name) }}" required><label>Company</label><input name="company_name" value="{{ old('company_name',$customer->company_name) }}"><div class="grid" style="grid-template-columns:1fr 1fr"><div><label>Email</label><input type="email" name="email" value="{{ old('email',$customer->email) }}" required></div><div><label>Phone</label><input name="phone" value="{{ old('phone',$customer->phone) }}"></div></div>@if($errors->any())<div class="error">{{ $errors->first() }}</div>@endif<button class="btn" style="margin-top:20px">Save customer</button></form></div>@endsection
