@extends('layout',['title'=>'Products & services · Quotely']) @section('content')<div class="top"><div><h1>Products & services</h1><p>Reusable items for faster quotation creation.</p></div><a class="btn" href="{{ route('products.create') }}">+ New item</a></div><div class="card"><table><thead><tr><th>Item</th><th>Price</th><th>Status</th><th></th></tr></thead><tbody>@forelse($products as $p)<tr><td><strong>{{ $p->name }}</strong><br><span class="muted">{{ $p->description }}</span></td><td>{{ auth()->user()->company->currency }} {{ number_format($p->unit_price_cents/100,2) }}</td><td><span class="badge">{{ $p->active?'Active':'Inactive' }}</span></td><td><a href="{{ route('products.edit',$p) }}">Edit</a></td></tr>@empty<tr><td colspan="4">No reusable items yet.</td></tr>@endforelse</tbody></table>{{ $products->links() }}</div>@endsection
