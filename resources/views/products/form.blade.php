@extends('layout',['title'=>($product->exists?'Edit':'New').' item · Quotely']) @section('content')<div style="max-width:700px;margin:auto"><div class="top"><h1>{{ $product->exists?'Edit':'New' }} item</h1><a class="btn secondary" href="{{ route('products.index') }}">Back</a></div><form class="card" method="post" action="{{ $product->exists?route('products.update',$product):route('products.store') }}">@csrf @if($product->exists)@method('put')@endif<label>Name</label><input name="name" value="{{ old('name',$product->name) }}" required><label>Description</label><textarea name="description">{{ old('description',$product->description) }}</textarea><label>Unit price ({{ auth()->user()->company->currency }})</label><input type="number" step=".01" min="0" name="unit_price" value="{{ old('unit_price',$product->exists?$product->unit_price_cents/100:'') }}" required><label><input style="width:auto" type="checkbox" name="active" value="1" {{ old('active',$product->active??true)?'checked':'' }}> Active</label>@if($errors->any())<div class="error">{{ $errors->first() }}</div>@endif<button class="btn" style="margin-top:20px">Save item</button></form></div>@endsection
