@extends('layout',['title'=>'Dashboard · Quotely']) @section('content')
<div class="top"><div><h1>{{ auth()->user()->company->name }}</h1><p>Track quotes, decisions, and revenue from one calm workspace.</p></div><a class="btn" href="{{ route('quotes.create') }}">+ New quotation</a></div>
<div class="grid"><div class="card"><div class="muted">Customers</div><div class="metric">{{ $customers }}</div></div><div class="card"><div class="muted">Accepted quotes</div><div class="metric">{{ $accepted }}</div></div><div class="card"><div class="muted">Outstanding</div><div class="metric">USD {{ number_format($outstanding/100,2) }}</div></div></div>
<div class="card" style="margin-top:20px"><h2>Recent quotations</h2><table><thead><tr><th>Quote</th><th>Customer</th><th>Total</th><th>Status</th><th></th></tr></thead><tbody>@forelse($quotes as $quote)<tr><td><strong>{{ $quote->number }}</strong><br><span class="muted">{{ $quote->title }}</span></td><td>{{ $quote->customer->name }}</td><td>{{ $quote->money($quote->total_cents) }}</td><td><span class="badge">{{ str_replace('_',' ',$quote->status) }}</span></td><td><a href="{{ route('quotes.show',$quote) }}">Open →</a></td></tr>@empty<tr><td colspan="5">No quotations yet. Create your first one.</td></tr>@endforelse</tbody></table></div>
@endsection
