@extends('layout',['title'=>'Start free trial · Quotely']) @section('content')<div style="max-width:520px;margin:45px auto"><div class="card"><h1>Start your 14-day trial</h1><p>No credit card required.</p><form method="post" action="{{ route('register') }}">@csrf<label>Business name</label><input name="company" value="{{ old('company') }}" required><label>Your name</label><input name="name" value="{{ old('name') }}" required><label>Work email</label><input type="email" name="email" value="{{ old('email') }}" required><label>Password</label><input type="password" name="password" required><small class="muted">At least 10 characters.</small><label>Confirm password</label><input type="password" name="password_confirmation" required>@if($errors->any())<div class="error" style="margin-top:12px">{{ $errors->first() }}</div>@endif<button class="btn" style="width:100%;margin-top:18px">Create workspace</button></form></div></div>@endsection
