@extends('layout',['title'=>'Sign in · Quotely']) @section('content')<div style="max-width:460px;margin:70px auto"><div class="card"><h1>Welcome back</h1><p>Sign in to your workspace.</p><form method="post" action="{{ route('login') }}">@csrf<label>Email</label><input type="email" name="email" value="{{ old('email') }}" required autofocus>@error('email')<div class="error">{{ $message }}</div>@enderror<label>Password</label><input type="password" name="password" required><label style="font-weight:400"><input style="width:auto" type="checkbox" name="remember" value="1"> Keep me signed in</label><button class="btn" style="width:100%;margin-top:14px">Sign in</button></form><p>New to Quotely? <a href="{{ route('register') }}">Start a free trial</a></p></div></div>@endsection
